packages:
  - "apps/*"
  - "packages/*"

# Native/binary packages explicitly allowed to run install scripts (pnpm >= 11).
allowBuilds:
  esbuild: true
  "@tailwindcss/oxide": true
  sharp: true
  unrs-resolver: true
